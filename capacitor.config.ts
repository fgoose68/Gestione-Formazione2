import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gestioneformazione',
  appName: 'Gestione Formazione',
  webDir: 'dist'
};

export default config;
